import type { ReactNode } from 'react'

/** Workspace overview stub — accepts any upstream props. */
export default function Workspace(_props: any): ReactNode {
  return null
}

export function WorkspacePanel(_props: any): ReactNode {
  return null
}
